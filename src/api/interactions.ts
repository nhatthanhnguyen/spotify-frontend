import { BASE_URL } from "./base";

const GROUP = `${BASE_URL}/interaction`;
export const LIKE_SONG = `${GROUP}/add`;
export const UNLIKE_SONG = `${GROUP}/remove`;
export const ADD_INTERACTIONS = `${GROUP}/add_new`;
export const DELETE_INTERACTIONS = `${GROUP}/remove_in/`;
